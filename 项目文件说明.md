# MIRA 项目文件说明文档

## 项目概述

MIRA (Medical Time Series Foundation Model) 是用于真实世界医疗时间序列的基础模型，支持零样本预测。本项目基于 Time-MoE 框架构建，包含训练、推理和评估功能。

**项目地址**: `/home/zjl/MIRA`

**代码来源**: 部分代码来自 Time-MoE (https://github.com/Time-MoE)，遵循 Apache License。

---

## 目录结构

```
/home/zjl/MIRA/
├── 根目录脚本
├── mira/                  # 核心代码包
│   ├── datasets/          # 数据集定义
│   ├── models/            # 模型定义
│   ├── trainer/           # 训练器
│   └── utils/             # 工具函数
├── ppg_output/            # 训练输出目录
│   ├── checkpoint-*/      # 模型检查点
│   ├── tb_logs/           # TensorBoard 日志
│   ├── loss_step*.png     # 训练损失图
│   ├── norm_stats.pkl     # 全局归一化参数
│   └── train_latest.log   # 最新训练日志
├── ppg_full/              # 训练数据目录
│   ├── ppg_data.npy       # memmap 训练数据（5.3GB）
│   ├── ppg_offsets.npy    # 序列偏移索引
│   ├── norm_stats.pkl     # 全局归一化参数（mean, std）
│   └── commom1_part_*.pkl # 原始 PKL 数据（89 个文件）
├── processed_dataset/     # 预处理/评估数据集
│   ├── split.json         # 数据集划分
│   └── data/              # 划分后的 PKL 文件
├── images/                # 模型架构图等
└── eval_results/          # 评估结果输出
    ├── eval_results.json  # 指标汇总
    ├── norm_stats.pkl     # 归一化参数
    └── predictions.png    # 预测可视化
```

---

## 一、根目录文件

### 1. 入口与训练脚本

#### `main.py` — 训练主入口
**作用**: 训练程序的命令行入口，负责解析所有训练参数并启动训练流程。

**主要功能**:
- 使用 `argparse` 解析 30+ 个训练参数（模型路径、数据路径、学习率、batch size 等）
- 自动检测 `output_path` 下的最新 checkpoint 并恢复训练
- 创建 `MIRARunner` 实例并调用 `train_model()`
- 默认模型: `Maple728/TimeMoE-50M`

**关键参数**:
- `--model_path`: 预训练模型路径
- `--data_path`: 训练数据路径（memmap 目录）
- `--learning_rate`: 学习率（默认 5e-5）
- `--micro_batch_size` / `--global_batch_size`: 批次大小
- `--gradient_checkpointing`: 梯度检查点（节省显存）
- `--from_scratch`: 从头训练（不加载预训练权重）

---

#### `train_loop.sh` — 自动重启训练脚本 ⭐
**作用**: 训练守护脚本，训练崩溃时自动从最新 checkpoint 恢复，最多重试 10 次。

**主要功能**:
- 自动检测 `ppg_output/checkpoint-*` 下的最新 checkpoint
- 有 checkpoint 则从中恢复，无则从头训练
- 捕获非零退出码，5 秒后自动重试
- 所有训练参数集中配置在脚本顶部

**当前配置**:
```bash
LEARNING_RATE="3e-5"      # 学习率
WARMUP_RATIO="0.1"        # warmup 比例 10%
ADAM_BETA2="0.999"        # Adam beta2
WEIGHT_DECAY="0.01"       # 权重衰减
SAVE_STEPS=500            # 每 500 步保存
```

**使用方式**:
```bash
cd /home/zjl/MIRA
bash train_loop.sh          # 前台运行
nohup bash train_loop.sh > ppg_output/train_latest.log 2>&1 &  # 后台运行
```

---

#### `stop_train.sh` — 一键停止训练
**作用**: 一键终止所有训练相关进程并释放端口。

**使用方式**:
```bash
bash stop_train.sh
```

---

### 2. 监控与可视化

#### `watch_progress.py` — 实时进度监控 ⭐
**作用**: 实时读取 `trainer_state.json`，在终端以美观的边框格式显示训练进度和预计剩余时间。

**显示内容**:
- 进度条（█░ 样式）、步数、epoch
- 最新 loss、学习率、梯度范数
- 训练速度和预计剩余时间
- 超参数摘要

**刷新频率**: 每 5 秒

**使用方式**:
```bash
python3 watch_progress.py
# 按 Ctrl+C 退出（训练继续在后台运行）
```

---

#### `plot_loss.py` — 自动生成 loss 图
**作用**: 监控训练日志，每 N 步（默认 500 步）自动生成一张 loss 曲线图。

**图表包含**:
- 完整 loss 曲线 + 滑动平均线
- 最近 500 步的放大图
- 梯度范数（对数刻度）
- 学习率调度曲线 + 训练统计信息

**输出位置**: `ppg_output/loss_step*.png`

**使用方式**:
```bash
python3 plot_loss.py   # 后台运行，自动监控
```

---

### 3. 数据预处理

#### `convert_pkl_to_npy.py` — PKL → memmap 转换 ⭐
**作用**: 将原始 PKL 文件转换为 numpy memmap 格式，加速训练时数据加载。

**主要功能**:
- 只提取 PPG 通道（索引 3），忽略 ACC X/Y/Z 三个通道
- 将所有序列拼接为一维 float32 数组（`ppg_data.npy`）
- 生成偏移索引数组（`ppg_offsets.npy`），记录每条序列的起始位置
- 最终生成约 5.3GB 的 memmap 文件

**使用方式**:
```bash
python3 convert_pkl_to_npy.py
```

---

#### `extract_norm_stats.py` — 全局归一化参数提取
**作用**: 从训练 memmap 数据中计算全局 mean 和 std，用于 z-score 归一化。

**主要功能**:
- 从 13.2 亿数据点中随机采样 5000 万点计算全局统计量
- 保存到 `ppg_full/norm_stats.pkl`（仅含 `global_mean` 和 `global_std`）
- 当前结果：mean = -28.5934, std = 6179.8335

**使用方式**:
```bash
python3 extract_norm_stats.py
```

---

### 4. 评估与推理

#### `run_eval.py` — 模型评估脚本 ⭐
**作用**: 对训练好的模型进行自回归预测评估，计算 RMSE、MAE、MAPE 指标，并生成可视化。

**主要功能**:
- 加载全局归一化参数（`norm_stats.pkl`），与训练时保持一致
- 从 PKL 加载测试序列，用全局 mean/std 做 z-score 归一化
- 自回归预测（300 点上下文 → 100 点预测）
- 用相同的全局参数反归一化，计算原始尺度指标
- 输出 `eval_results.json`（指标）和 `predictions.png`（可视化）

**评估设置**:
- Context: 300 点，Predict: 100 点
- 默认评估 200 个测试样本

**使用方式**:
```bash
python3 run_eval.py --checkpoint ppg_output/checkpoint-7000 --data processed_dataset/data
```

---

#### `model_eval.py` — 另一种评估实现
**作用**: 与 `eval_model.py` 功能类似，从不同代码路径实现。

---

#### `eval_model.py` — 多尺度评估
**作用**: 支持多组 context/prediction 长度设置的综合评估。

**评估设置**:
- 48→24 (480ms → 240ms)
- 96→48 (960ms → 480ms)
- 128→64 (1.28s → 640ms)
- 256→128 (2.56s → 1.28s)

---

### 5. 分布式运行

#### `torch_dist_run.py` — 分布式运行包装器
**作用**: 简化 torchrun 命令的封装，自动解析任意命令行参数并传递给目标脚本。

**主要功能**:
- `parse_arbitrary_args()`: 解析 `--key value` 格式的参数
- `obtain_dist_env_dict()`: 获取分布式环境变量
- `auto_dist_run()`: 自动配置并执行 torchrun

**使用方式**:
```bash
python3 torch_dist_run.py main.py --model_path xxx --data_path yyy
```

---

### 6. 配置文件

#### `setup.py` — 包安装配置
**作用**: Python 包安装配置，定义包名为 `mira`，版本 1.0。

```bash
pip install -e .  # 开发模式安装
```

---

#### `README.md` — 项目说明
**作用**: 项目 README，包含模型概述、特性介绍、安装指南等。

**关键信息**:
- 模型发表于 NeurIPS '25
- 预训练数据: 454B 时间序列数据点
- 三大特性: CT-RoPE 位置编码、频率专用 MoE、神经 ODE 外推

---

#### `NOTICE.md` — 第三方组件声明
**作用**: 声明项目使用的第三方组件及其许可证。

**包含组件**:
- Time-MoE 训练/推理框架 (Apache License)
- LLaMA 注意力机制 (LLAMA 2 Community License)
- Mistral 位置嵌入 (Apache License)
- SwitchTransformers 损失函数 (MIT License)

---

#### `SUPPORT.md` — 支持文档
**作用**: Microsoft 标准支持文档模板。

---

#### `.claude/settings.local.json` — Claude Code 配置
**作用**: Claude Code 工具的本地配置文件。

---

## 二、mira/ 核心代码包

### mira/runner.py — 核心训练器 ⭐
**作用**: 整个训练流程的核心控制器，负责加载模型、数据集、配置训练参数并执行训练。

**主要类**: `MIRARunner`

**核心方法**:
- `load_model()`: 加载预训练模型或从头初始化。自动检测 Flash Attention 可用性
- `train_model()`: 设置数据集、训练参数、创建 Trainer 并执行训练

**数据集选择优先级**: memmap（最快） > PKL > JSONL > 普通格式

**辅助函数**:
- `setup_seed()`: 设置所有随机种子（Python/NumPy/PyTorch）
- `length_to_str()`: 格式化大数字（K/M/B/T）

---

### mira/trainer/hf_trainer.py — 自定义训练器 ⭐
**作用**: 继承 HuggingFace `Trainer`，自定义损失计算、学习率调度器和 checkpoint 恢复逻辑。

**主要类**: `MIRATrainer`, `MIRATrainingArguments`

**关键功能**:
1. **自定义 Loss**: 支持 loss mask，忽略 padding 位置的损失
2. **余弦退火 + Warmup**: 实现 `_get_cosine_schedule_with_warmup_and_min_lr_lambda`
3. **Checkpoint 恢复**: 覆盖 `_load_from_checkpoint`，修复 `max_steps` 不匹配问题
4. **训练参数**: `MIRATrainingArguments` 扩展了 `min_learning_rate` 和 `warmup_start_lr`

---

### mira/models/modeling_mira.py — 模型定义 ⭐
**作用**: MIRA 模型的 PyTorch 实现，包含完整的 Transformer + MoE + ODE 架构。

**主要类**:
- `MIRAForPrediction`: 顶层预测模型
- `MIRAModel`: 基础 Transformer 模型
- `MIRADecoderLayer`: Transformer 解码层
- `MIRAAttention`: 自注意力机制
- `MIRAMoEBlock`: Mixture-of-Experts 模块
- `MIRAMLP`: MLP 层

**架构**:
- 12 层 Transformer
- 隐藏层: 384
- 注意力头: 12 (GQA)
- 专家数: 8 (每次激活 2 个)
- 参数量: 113.428M
- CT-RoPE 位置编码
- Terminal ODE 求解器 (dopri5)

---

### mira/models/configuration_mira.py — 模型配置
**作用**: 定义模型的所有超参数，继承 HuggingFace `PretrainedConfig`。

**关键参数**:
- `hidden_size`: 隐藏层维度
- `num_hidden_layers`: Transformer 层数
- `num_attention_heads`: 注意力头数
- `num_experts`: MoE 专家数
- `max_position_embeddings`: 最大位置编码长度
- `rope_theta`: RoPE 温度参数
- `time_aware_rotary`: 是否使用 CT-RoPE
- `use_terminal_ode`: 是否使用终端 ODE

---

### mira/models/utils_time_normalization.py — 时间归一化工具
**作用**: 为 CT-RoPE 提供时间归一化功能。

---

### mira/models/mira_inference.py — 推理工具
**作用**: 提供模型推理的辅助函数。

---

### mira/models/ts_generation_mixin.py — 序列生成
**作用**: 继承 HuggingFace `GenerationMixin`，提供时间序列生成能力。

---

### mira/datasets/ts_dataset.py — 数据集基类
**作用**: 定义所有数据集的抽象接口。

---

### mira/datasets/memmap_dataset.py — Memmap 数据集 ⭐
**作用**: 使用 numpy memmap 实现的高速数据集，训练时实际使用。

**主要类**:
- `MIRADataMemmapDataset`: 从 `ppg_data.npy` 加载数据，计算全局 mean/std 做 z-score 归一化
- `FlatWindowDataset`: 将序列切分为固定长度窗口，预计算所有窗口偏移，应用相同的全局归一化

**归一化方式**: 全局 z-score — 从所有训练数据计算统一的 mean/std，保存到 `norm_stats.pkl`

**性能**: 直接从内存映射文件读取，无需解析 PKL，O(1) 随机访问。

**数据格式**: `ppg_data.npy`（所有序列拼接为一维数组）+ `ppg_offsets.npy`（每条序列的偏移索引）

---

### mira/datasets/mira_dataset.py — 通用数据集
**作用**: 从 PKL/JSON/YAML 等格式加载时间序列数据的通用数据集。

---

### mira/datasets/mira_window_dataset.py — 窗口数据集
**作用**: 定义窗口化的数据集。

---

### mira/datasets/timeawared_dataset.py — 时间感知 JSONL 数据集
**作用**: 从 JSONL 文件加载带时间戳的时间序列数据。

---

### mira/datasets/timeawared_pkl_dataset.py — 时间感知 PKL 数据集
**作用**: 从 PKL 文件加载带时间戳的时间序列数据。

---

### mira/datasets/benchmark_dataset.py — 基准评估数据集
**作用**: 用于在标准基准数据集上评估模型性能。

---

### mira/datasets/general_dataset.py — 通用数据集
**作用**: 通用的时间序列数据集实现。

---

### mira/datasets/time_utils.py — 时间工具
**作用**: 时间相关的工具函数，如时间感知 collate 函数。

---

### mira/utils/dist_util.py — 分布式工具
**作用**: 分布式训练的辅助函数。

---

### mira/utils/log_util.py — 日志工具
**作用**: 统一的日志记录工具，仅在 rank 0 进程输出日志。

---

## 三、输出目录

### ppg_output/ — 训练输出
```
ppg_output/
├── checkpoint-500/          # 每 500 步的 checkpoint
│   ├── model.safetensors    # 模型权重 (~227MB)
│   ├── trainer_state.json   # 训练状态（步数、loss 历史等）
│   └── training_args.bin    # 训练参数
├── tb_logs/                 # TensorBoard 日志
├── loss_step500.png         # 每 500 步的 loss 图
├── norm_stats.pkl           # 全局归一化参数（训练时生成）
└── train_latest.log         # 最新训练日志
```

---

## 四、数据目录

### ppg_full/ — PPG 训练数据
```
ppg_full/
├── ppg_data.npy             # 所有序列拼接的 memmap 数据（5.3GB, float32）
├── ppg_offsets.npy          # 每条序列的偏移索引（int64）
├── norm_stats.pkl           # 全局归一化参数: {global_mean: -28.59, global_std: 6179.83}
└── commom1_part_001.pkl     # 原始 PKL 数据（89 个文件, 4通道）
```

### processed_dataset/ — 评估数据集
```
processed_dataset/
├── split.json               # 数据集划分（train/test/val）
└── data/                    # 划分后的 PKL 文件（1000 个）
```

---

## 五、数据预处理流程

```
原始 PKL (4通道: ACC X/Y/Z + PPG)
    ↓ [convert_pkl_to_npy.py] 只提取 PPG 通道
ppg_data.npy (一维 float32, 13.2亿点) + ppg_offsets.npy
    ↓ [extract_norm_stats.py] 计算全局 mean/std
norm_stats.pkl {global_mean: -28.59, global_std: 6179.83}
    ↓ [训练时] FlatWindowDataset 用全局参数归一化
归一化窗口 → 模型训练
    ↓ [评估时] run_eval.py 用相同的全局参数
归一化测试序列 → 模型预测 → 反归一化求指标
```

---

## 六、启动流程总结

```
bash train_loop.sh
    ↓
main.py (解析参数，创建 MIRARunner)
    ↓
mira/runner.py (加载模型 + 数据集，配置 Trainer)
    ↓
mira/trainer/hf_trainer.py (自定义训练循环)
    ↓
mira/models/modeling_mira.py (模型 forward)
    ↓
mira/datasets/memmap_dataset.py (数据加载 + 全局归一化)
```

**监控方式**:
```bash
python3 watch_progress.py   # 实时进度
python3 plot_loss.py        # 自动生成 loss 图
tensorboard --logdir ppg_output/tb_logs  # TensorBoard
```

**停止方式**:
```bash
bash stop_train.sh
```

**评估方式**:
```bash
python3 run_eval.py  # 使用 checkpoint-7000 评估
```
