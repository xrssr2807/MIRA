# MIRA 模型训练指南

## 1. 环境要求

- Python 3.11+
- PyTorch + CUDA
- HuggingFace Transformers
- 推荐硬件: 单卡 NVIDIA GPU (24GB+ 显存)

## 2. 项目结构

```
/home/zjl/MIRA/
├── main.py                 # 训练入口，解析参数
├── train_loop.sh           # 自动重启脚本（崩溃后自动恢复）
├── convert_pkl_to_npy.py   # PKL → memmap 转换（只取 PPG 通道）
├── extract_norm_stats.py   # 提取全局归一化参数
├── run_eval.py             # 模型评估脚本
├── watch_progress.py       # 实时训练进度监控
├── plot_loss.py            # 自动生成 loss 图
├── mira/
│   ├── runner.py           # 核心训练逻辑
│   ├── trainer/            # 自定义 Trainer + LR 调度器
│   ├── models/             # MIRA 模型定义
│   └── datasets/           # 数据集定义（含归一化逻辑）
└── ppg_full/               # 训练数据目录
    ├── ppg_data.npy        # memmap 训练数据（5.3GB）
    ├── ppg_offsets.npy     # 序列偏移索引
    └── norm_stats.pkl      # 全局归一化参数
```

## 3. 数据预处理

### 3.1 PKL → memmap 转换

```bash
python3 convert_pkl_to_npy.py
```

将 `ppg_full/` 下 89 个 PKL 文件转换为 memmap 格式：
- **只提取 PPG 通道**（索引 3），忽略 ACC X/Y/Z
- 生成 `ppg_data.npy`（5.3GB）+ `ppg_offsets.npy`

### 3.2 计算全局归一化参数

```bash
python3 extract_norm_stats.py
```

从训练 memmap 中采样 5000 万点计算：
```
global_mean = -28.5934
global_std  =  6179.8335
```
保存到 `ppg_full/norm_stats.pkl`，训练和评估共用。

### 3.3 归一化方式

采用**全局 z-score 归一化**：

```python
# 归一化
x_norm = (x - global_mean) / global_std

# 反归一化
x_orig = x_norm * global_std + global_mean
```

- 训练时：窗口数据用全局 mean/std 归一化
- 评估时：测试序列用相同的全局 mean/std 归一化
- 模型只在归一化空间工作，反归一化由下游任务完成

## 4. 启动训练

### 方式一：使用自动重启脚本（推荐）

自动重启脚本会在训练崩溃时自动恢复，最多重试 10 次，始终从最新 checkpoint 恢复。

```bash
cd /home/zjl/MIRA
bash train_loop.sh
```

### 方式二：直接启动

```bash
cd /home/zjl/MIRA
torchrun --master_addr=localhost --master_port=9899 \
    --node_rank=0 --nproc_per_node=1 --nnodes=1 \
    main.py \
    --model_path Maple728/TimeMoE-50M \
    --data_path ppg_full \
    --output_path ppg_output \
    --max_length 1024 \
    --normalization_method zero \
    --attn_implementation eager \
    --micro_batch_size 52 \
    --global_batch_size 52 \
    --precision bf16 \
    --save_strategy steps \
    --save_steps 500 \
    --gradient_checkpointing \
    --save_only_model \
    --save_total_limit 5 \
    --logging_steps 10 \
    --lr_scheduler_type cosine \
    --warmup_ratio 0.1 \
    --learning_rate 3e-5 \
    --adam_beta2 0.999 \
    --weight_decay 0.01 \
    --num_train_epochs 1.0 \
    --dataloader_num_workers 2
```

## 5. 参数说明

### 模型与数据

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--model_path` | `Maple728/TimeMoE-50M` | 预训练模型路径 |
| `--data_path` | - | 训练数据目录（含 `ppg_data.npy`） |
| `--output_path` | `logs/time_moe` | 模型输出和 checkpoint 保存路径 |
| `--max_length` | `1024` | 序列窗口长度 |
| `--normalization_method` | `zero` | 数据归一化方法 (`none`, `zero`, `max`)。`zero` 时使用全局 z-score 归一化 |

### 训练参数

| 参数 | 当前值 | 说明 |
|------|--------|------|
| `--precision` | `bf16` | 精度模式 |
| `--micro_batch_size` | `52` | 每卡 micro batch size |
| `--global_batch_size` | `52` | 全局 batch size |
| `--gradient_accumulation_steps` | 自动计算 | 梯度累积步数 |
| `--num_train_epochs` | `1.0` | 训练轮数 |
| `--train_steps` | `None` | 指定训练步数（覆盖 epochs） |
| `--gradient_checkpointing` | 开启 | 启用梯度检查点以节省显存 |
| `--dataloader_num_workers` | `2` | DataLoader worker 数量 |

### 学习率调度

| 参数 | 当前值 | 说明 |
|------|--------|------|
| `--learning_rate` | `3e-5` | 基础学习率 |
| `--lr_scheduler_type` | `cosine` | 余弦衰减 |
| `--warmup_ratio` | `0.1` | warmup 占总步数比例（10%） |
| `--warmup_start_lr` | `1e-4` | warmup 起始 LR 比值 |
| `--adam_beta2` | `0.999` | Adam beta2 |
| `--weight_decay` | `0.01` | 权重衰减 |

### 检查点与日志

| 参数 | 当前值 | 说明 |
|------|--------|------|
| `--save_strategy` | `steps` | 保存策略 |
| `--save_steps` | `500` | 每 500 步保存一次 |
| `--save_total_limit` | `5` | 最多保留 5 个 checkpoint |
| `--save_only_model` | 开启 | 只保存模型权重 |
| `--logging_steps` | `10` | 每 10 步打印一次 loss |
| `--from_scratch` | 关闭 | 加载预训练权重 |

## 6. GPU 调优建议

### 显存优化

- 开启 `--gradient_checkpointing`：减少约 60% 显存占用
- 推荐配置：`--micro_batch_size 52`（单卡 24GB 显存）
- 显存占用约 20GB/24GB（81%），GPU 利用率 100%
- 速度约 1.3 秒/步

### 如果遇到 OOM（显存不足）

1. 减小 `--micro_batch_size`（从 52 降到 32 或更低）
2. 确保开启 `--gradient_checkpointing`
3. 使用 `bf16` 而非 `fp32`

### 数据加载加速

- `--dataloader_num_workers 2`：当前最优值，worker 过多反而拖慢速度
- memmap 格式比 PKL 快数十倍

## 7. 训练恢复

### 从 checkpoint 恢复

训练脚本会自动检测 `output_path` 下的最新 checkpoint 并恢复：

```bash
# train_loop.sh 会自动检测并恢复
bash train_loop.sh
```

Checkpoint 格式：`ppg_output/checkpoint-5000/`

### 停止训练

```bash
bash stop_train.sh
```

## 8. 监控训练

```bash
# 实时进度（进度条 + ETA）
python3 watch_progress.py

# 自动生成 loss 图（每 500 步）
python3 plot_loss.py

# TensorBoard
tensorboard --logdir ppg_output/tb_logs

# GPU 状态
nvidia-smi
```

## 9. 模型评估

```bash
python3 run_eval.py
```

评估配置：
- 使用全局归一化参数（与训练一致）
- Context: 300 点 → Predict: 100 点
- 默认评估 200 个测试样本
- 输出：`ppg_output/eval_results/eval_results.json` + `predictions.png`

## 10. 常见问题

### 端口被占用

```bash
# 释放端口
kill $(lsof -t -i:9899)
```

### 预训练模型下载失败

```bash
export HF_HUB_OFFLINE=1  # 离线模式
```

### 训练速度异常慢

Checkpoint 恢复阶段会重建数据迭代器，速度较慢（约 5 it/s），这是正常现象。越过 checkpoint 后速度恢复正常（约 1.3 秒/步）。

### 归一化参数不匹配

训练和评估必须使用相同的全局 mean/std。检查 `ppg_full/norm_stats.pkl` 是否存在且正确。

## 11. 模型信息

- **参数量**: 113.428M
- **架构**: Mixture-of-Experts (MoE)
- **专家数**: 8（每次激活 2 个）
- **层数**: 12 Transformer 层
- **隐藏层**: 384
- **注意力头数**: 12
- **位置编码**: CT-RoPE（连续时间旋转位置编码）
- **求解器**: dopri5 ODE 求解器
